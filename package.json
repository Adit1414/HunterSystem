{
    "name": "hunter-system-unified",
    "version": "1.0.0",
    "type": "module",
    "scripts": {
        "install-all": "npm install --prefix backend && npm install --prefix frontend",
        "build-frontend": "npm run build --prefix frontend",
        "start": "node backend/src/server.js",
        "replit-setup": "npm run install-all && npm run build-frontend && npm run start"
    },
    "dependencies": {
        "express": "^4.18.2"
    },
    "engines": {
        "node": ">=18"
    }
}